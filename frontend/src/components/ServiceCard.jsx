import React from 'react';
import { ArrowUpRight, BadgeCheck } from 'lucide-react';
export default function ServiceCard({service}){const open=()=>window.open(service.officialUrl,'_blank','noopener,noreferrer');return <article className="service-card" onClick={open} onKeyDown={e=>(e.key==='Enter'||e.key===' ')&&open()} tabIndex="0" role="link"><div className="card-top"><div className="portal-logo">{service.logo||service.portalName.slice(0,2)}</div><div><p className="portal">{service.portalName}</p><span className="official"><BadgeCheck/> Official Government Service</span></div></div><h3>{service.serviceName}</h3><p className="description">{service.description}</p><div className="card-footer"><span className="badge">{service.category}</span><button onClick={e=>{e.stopPropagation();open()}}>{service.buttonText}<ArrowUpRight/></button></div></article>}
