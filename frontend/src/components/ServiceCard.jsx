import React, { useState } from 'react';
import { ArrowUpRight, BadgeCheck } from 'lucide-react';
import RedirectConfirmation from './RedirectConfirmation.jsx';
import { t } from '../i18n.js';
export default function ServiceCard({service,primary=false,locale='en'}){const[confirming,setConfirming]=useState(false);const open=()=>{setConfirming(false);window.open(service.officialUrl,'_blank','noopener,noreferrer')};return <><article className={`service-card ${primary?'primary-service':''}`} style={primary?{borderColor:'#8eb2e5',boxShadow:'0 16px 38px rgba(26,79,150,.13)'}:undefined} onClick={()=>setConfirming(true)} onKeyDown={e=>(e.key==='Enter'||e.key===' ')&&setConfirming(true)} tabIndex="0" role="link"><div className="card-top"><div className="portal-logo">{service.logo||service.portalName.slice(0,2)}</div><div><p className="portal">{service.issuerName||service.portalName}</p><span className="official"><BadgeCheck/> {service.documentMatch?'Exact document in DigiLocker':t(locale,'official')}</span></div></div><h3>{service.serviceName}</h3><p className="description">{service.description}</p><div className="card-footer"><span className="badge">{service.category}</span><button onClick={e=>{e.stopPropagation();setConfirming(true)}}>{primary?t(locale,'go'):service.buttonText}<ArrowUpRight/></button></div></article>{confirming&&<RedirectConfirmation service={service} onCancel={()=>setConfirming(false)} onConfirm={open}/>}</>}
