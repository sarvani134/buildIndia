import React, { useState } from 'react';
import { ArrowUpRight, BadgeCheck } from 'lucide-react';
import RedirectConfirmation from './RedirectConfirmation.jsx';
export default function ServiceCard({service}){const[confirming,setConfirming]=useState(false);const open=()=>{setConfirming(false);window.open(service.officialUrl,'_blank','noopener,noreferrer')};return <><article className="service-card" onClick={()=>setConfirming(true)} onKeyDown={e=>(e.key==='Enter'||e.key===' ')&&setConfirming(true)} tabIndex="0" role="link"><div className="card-top"><div className="portal-logo">{service.logo||service.portalName.slice(0,2)}</div><div><p className="portal">{service.portalName}</p><span className="official"><BadgeCheck/> Official Government Service</span></div></div><h3>{service.serviceName}</h3><p className="description">{service.description}</p><div className="card-footer"><span className="badge">{service.category}</span><button onClick={e=>{e.stopPropagation();setConfirming(true)}}>{service.buttonText}<ArrowUpRight/></button></div></article>{confirming&&<RedirectConfirmation service={service} onCancel={()=>setConfirming(false)} onConfirm={open}/>}</>}
